#!/usr/bin/node
let fs = require('fs');
let process = require('process');
var args = process.argv;
console.log("number of arguments is: " +args.length);
console.log("and the second argument is: " +args[2]);
